# Ebook placeholder

The final ebook PDF goes here as `afrodite-prazer-7-etapas.pdf`.

The `/api/subscribe` route emails leads a link to `EBOOK_DOWNLOAD_URL` (env var,
defaults to `/ebook/afrodite-prazer-7-etapas.pdf`).

Until Cecília delivers the diagrammed PDF, dev requests will email a 404 link.
That is fine for scaffolding — replace this file with the real PDF before launch.
